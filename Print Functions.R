# You can output code in R without using a print function.
# However, using print() can be helpful if you are familiar with other languages like Python.
# Sometimes print() is necessary to output code, e.g: when implementing for loops.
for (x in 1:10){print(x)}
